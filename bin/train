#!/usr/bin/env ruby
# bulk-loads a dataset from a text file

$: << File.join(File.dirname(__FILE__), "..")

require 'lib/datafile'
require 'lib/redis_bayes'

def main
  file = DataFile.new(ARGV.first)
  rb = RedisBayes.new
  test, train = file.split(4, true)
  rb.train(train)
end

main
